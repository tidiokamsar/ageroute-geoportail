# Déploiement du 05/09/2026 — 18:28 UTC

**Répertoire** : `/opt/console-bdri` (serveur `gec`) · **Commits** : `9dd8ff2`, `f9f9fdc`
**Sauvegarde préalable** : `20260905_181909` — intégrité vérifiée, copie hors serveur effectuée

## Ce qui a été mis en ligne

| Commit | Objet |
|---|---|
| `9dd8ff2` | Les huit constats de la revue du 05/09 |
| `f9f9fdc` | Cadrage des ouvrages sur la carte publique (déployé seul à 11:20, avant le reste) |

`f9f9fdc` est parti seul et en premier, parce qu'il corrigeait une exposition en cours.
`9dd8ff2` a attendu la décision sur les modules — le déployer avant aurait coupé l'accès
au gestionnaire et au lecteur.

## Attribution des modules — préalable obligatoire

`9dd8ff2` change le sens d'une liste de modules vide : elle valait « aucune
restriction », elle vaut « aucun module ». Les deux comptes non-ADMIN actifs étaient
tous deux dans ce cas, donc atteignaient tout.

Les 16 modules leur ont été attribués **avant** le déploiement. Sous l'ancien code une
liste pleine ne changeait rien pour eux : il n'y a donc eu aucune fenêtre de blocage.

```
oumou.barry@ageroute.gov.gn     GESTIONNAIRE   0 -> 16 modules
suivi@ageroute.gov.gn           LECTEUR        0 -> 16 modules
```

`utilisateurs` et `administration` ne figurent pas dans `MODULE_KEYS` : ils sont
réservés au rôle ADMIN sans dérogation possible. L'exclusion demandée était donc déjà
structurelle, et ces deux comptes n'y ont jamais eu accès.

La profondeur d'accès reste celle du rôle : un LECTEUR lit, un GESTIONNAIRE écrit. La
liste de modules dit seulement quelles sections sont visibles, pas ce qu'on peut y faire.

## Import des franchissements, le même jour

349 ouvrages créés, 614 écartés comme déjà présents — inventaire porté à **1 089**
(126 inventoriés par AGEROUTE + 963 repris d'une source cartographique externe).

Le premier import, le matin, n'en avait écrit que 614 sur 963 : la clé retenue était le
champ `numero`, qui porte le numéro de la ROUTE et non de l'ouvrage. 376 ponts n'en
portaient que 27 valeurs distinctes, dont 110 sur « N1 ». `ON CONFLICT DO NOTHING` a
donc avalé 349 lignes sans un mot. La clé est devenue la position seule.

Les 27 lignes déjà écrites sous une clé fautive ont été **re-clés, pas supprimées** :
leur correspondance a été vérifiée avant écriture (écart mesuré 0,000 m).

## Vérifications après mise en ligne

Bundle compilé dans le conteneur :

```
RESEAU_CLASSE (liste des tronçons cadrée)          3
balayerTwoFa  (compteur 2FA par utilisateur)       2
hasOwnProperty (garde du journal d'audit)          1
modulesAutorises.length === 0 (ancien compotement) 0
```

Sonde `/api/health` : `api ok`, `base ok 7 ms`, `stockage ok 4 ms`, 0 échec d'audit.

Carte publique, mesurée en direct :

```
ouvrages  : 124  {BON 103, MOYEN 9, MAUVAIS 10, CRITIQUE 2}
tronçons  : 1691
chantiers : 479
charge    : 754 060 octets
```

Aucun ouvrage en `NON_EVALUE` : les 963 reprises ne sont plus servies au public. Les
124 sont les 126 inventoriés moins deux qui n'ont pas de géométrie — écart antérieur,
non introduit ici.

## Reste ouvert

- Le mot de passe ADMIN transmis en conversation n'a pas été changé.
- La clé de chiffrement des sauvegardes (`~/.bdri/cle-sauvegarde`) n'a toujours aucune
  copie hors serveur : les archives déposées à l'extérieur sont illisibles sans elle.
- `test.gestionnaire@ageroute.gov.gn` porte le rôle **ADMIN** en production. Le nom
  suggère un compte d'essai ; le rôle lui donne tout, y compris les comptes utilisateurs.
- 19 des 963 ponts repris restent en région « Non renseigné » : leur position tombe hors
  des polygones administratifs, tous à moins de 3,3 km d'une limite. Cas de bordure
  réels — les rattacher au plus proche serait deviner.
